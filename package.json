{
  "name": "postpandemic",
  "version": "1.0.0",
  "description": "Messages of hope for a world recovering from the Coronavirus Pandemic.",
  "main": "index.js",
  "repository": "git@github.com:lukekyl/react-portfolio-project.git",
  "author": "Kyle Luke",
  "license": "MIT",
  "scripts": {
    "build": "cd backend && yarn install && yarn build && cd ..",
    "deploy": "cp -a backend/build/. public/",
    "postinstall": "yarn build && yarn deploy && echo 'Client built!'"
  },
  "engines": {
    "yarn": "1.22.0",
    "npm": "6.14.5",
    "node": "10.20.0"
  }
}

